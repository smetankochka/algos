#include <iostream>
#include <vector>

using namespace std;

struct trio{
    int n, l, r;
    trio() {n = -1; l = -1; r = -1; }
    trio(int n, int l, int r) { this->n = n; this->l = l; this->r = r; }
};

int main() {
    int n;
    cin >> n;
    
    return 0;
}